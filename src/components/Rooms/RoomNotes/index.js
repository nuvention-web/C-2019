import React, { Component } from 'react';
import PropTypes from 'prop-types';

class RoomNotes extends Component {
    render() {
        return (
           <div></div>
        );
    }
}

export default RoomNotes;